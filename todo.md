# Список задач (TODO)

## 1. Блок мини-галереи (Главная страница)
- Добавить секцию с мини-галереей на главную страницу (`app/pages/index.vue`).
- Отображать 3-4 фотографии.
- Добавить кнопку-ссылку для перехода на страницу `/gallery`.

## 2. Блок "Частые вопросы" (FAQ)
- Создать секцию FAQ (возможно в виде отдельного компонента или прямо на главной странице).
- Использовать компонент аккордеона для отображения вопросов и ответов.

## 3. Футер
- Разработать современный, минималистичный футер в `app.vue` или как компонент, подключаемый на все страницы.
- Установить цвет фона `secondary` (используя классы Tailwind/Nuxt UI).
- Добавить необходимые навигационные ссылки, копирайт и т.д.

## 4. Навигация по секциям (ScrollSpy / Intersection Observer)
- Каждой ключевой секции на главной странице (`app/pages/index.vue`) присвоить уникальный атрибут `id`.
- Реализовать логику отслеживания прокрутки (например, с помощью `Intersection Observer API` или `vueuse`), чтобы URL хэш (`/#имя-секции`) автоматически обновлялся в зависимости от того, какая секция сейчас находится в области видимости.